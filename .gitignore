# Dependencias y build
node_modules/
.next/
next-env.d.ts
tsconfig.tsbuildinfo

# Entorno: los secretos NUNCA se suben.
# `.env.example` sí, es la plantilla que documenta qué hace falta.
.env
.env.*
!.env.example

# Base de datos local (PGlite)
.pglite/

# Playwright
test-results/
playwright-report/
.vercel
.env*

# PGlite se copia desde node_modules en cada build (scripts/copy-pglite.ts)
/public/pglite/
/public/vendor/
/public/php/
